from distutils.core import setup

setup(
    name = 'Simulated_Store_Data_System',
    version = '0.1dev',
    packages = ['src', 'tests'],
    long_description = open('README.md').read(),
    install_requires = []
    )
